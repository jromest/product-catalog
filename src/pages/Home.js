import React from 'react';
import Layout from '../components/Layout';

const Home = props => (
  <Layout location={props.location}>
    <main>
      <h1>Home</h1>
    </main>
  </Layout>
);

export default Home;
